# BinusMaya

Student portal demo: dashboard, schedule, courses, and forum in one place.

## Run

Open `index.html` in a browser, or serve it:

```sh
python3 -m http.server 8000
# → http://localhost:8000/
```

Demo login: NIM `2502001234` · password `password123`. No data leaves the browser (localStorage only).
